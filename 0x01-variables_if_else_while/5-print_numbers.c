#include <stdio.h>

/**
 * main - function to print number of base 10
 * followed bu a new line
 * Return: 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");

	return (0);
}
