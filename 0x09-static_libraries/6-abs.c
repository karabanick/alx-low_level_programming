#include "main.h"

/**
 * _abs - Function that computes the absolute value of an integer
 * @x: Integer parameter
 * Return: Result
 */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
