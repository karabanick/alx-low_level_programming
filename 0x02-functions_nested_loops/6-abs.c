#include "main.h"

/**
 * _abs - function to compute absolute value of an int
 * @n: funtion parameter
 * Return: result
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
